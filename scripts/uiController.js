/** * @file uiController.js * @description Manages all UI interactions, event listeners, state syncing, * settings persistence, and mockup text/style preview. */ import { quranService } from './quranService.js'; import { languageManager } from './languageManager.js'; import { unsplashService } from './unsplashService.js'; /** * Helper to combine a range of fetched verses into a unified, beautifully styled layout data payload. */ function combineVerses(verses, secondLanguage) { if (verses.length === 1) { return { ...verses[0], verses: verses }; } const first = verses[0]; const last = verses[verses.length - 1]; const firstNum = parseInt(first.verseKey.split(':')[1]); const lastNum = parseInt(last.verseKey.split(':')[1]); const verseKey = `${first.verseKey.split(':')[0]}:${firstNum}-${lastNum}`; const toArabicDigits = (num) => { const arabicDigits = ['٠', '١', '٢', '٣', '٤', '٥', '٦', '٧', '٨', '٩']; return String(num).split('').map(d => arabicDigits[parseInt(d)] || d).join(''); }; const arabic = verses.map(v => { const num = v.verseKey.split(':')[1]; return `${v.arabic} ﴿${toArabicDigits(num)}﴾`; }).join(" "); const en = verses.map(v => v.translations.en).join(" "); const translations = { en }; if (secondLanguage !== 'en') { const secondaryTextList = verses.map(v => v.translations[secondLanguage] || "").filter(Boolean); translations[secondLanguage] = secondaryTextList.join(" "); } return { surahName: first.surahName, verseKey, arabic, translations, audio: first.audio, verses: verses }; } export class UIController { constructor() { this.state = { surah: 18, startVerse: 10, endVerse: 10, translation: 'id', bgType: 'image', searchQuery: 'nature landscape stars', contrast: 45, arabicFont: 'Amiri', translationFont: 'Plus Jakarta Sans', arabicSize: 32, translationSize: 16, diacritics: true, goldenBorder: true, showReference: true, reciter: 'Mishary Rashid Alafasy', visualizer: 'Classic Bars', aspectRatio: '1:1', bgSeed: 12345, activeVerseData: null, isPlayingAudio: false, currentVerseIdx: 0, fontGap: 0, lineGap: 0, theme: 'dark' }; if (typeof Audio !== 'undefined') { this.audioElement = new Audio(); this.audioElement.crossOrigin = 'anonymous'; } else { this.audioElement = { addEventListener: () => {}, removeEventListener: () => {}, load: () => {}, pause: () => {}, play: () => Promise.resolve(), duration: 15, currentTime: 0 }; } this.els = {}; this.chapters = []; } initElements() { this.els = { surahSelect: document.getElementById('surahSelect'), startVerseInput: document.getElementById('startVerseInput'), endVerseInput: document.getElementInput('endVerseInput'), translationSelect: document.getElementById('translationSelect'), fetchButton: document.getElementById('fetchButton'), contrastSlider: document.getElementById('contrastSlider'), arabicFontSelect: document.getElementById('arabicFontSelect'), translationFontSelect: document.getElementById('translationFontSelect'), arabicSizeSlider: document.getElementById('arabicSizeSlider'), translationSizeSlider: document.getElementById('translationSizeSlider'), diacriticsCheckbox: document.getElementById('diacriticsCheckbox'), goldenBorderCheckbox: document.getElementById('goldenBorderCheckbox'), showReferenceCheckbox: document.getElementById('showReferenceCheckbox'), fontGapSlider: document.getElementById('fontGapSlider'), lineGapSlider: document.getElementById('lineGapSlider'), shuffleBgBtn: document.getElementById('shuffleBgBtn'), reciterSelect: document.getElementById('reciterSelect'), ratioButtons: document.querySelectorAll('#previewWorkspace > div:first-of-type button'), mockupContainer: document.getElementById('mockupContainer'), canvas: document.getElementById('studioCanvas'), generateImageBtn: document.getElementById('generateImageBtn'), generateVideoBtn: document.getElementById('generateVideoBtn'), audioPlayBtn: document.querySelector('#previewWorkspace .absolute.bottom-4 button'), audioTimer: document.querySelector('#previewWorkspace .font-mono'), audioWaveIndicator: document.querySelector('#previewWorkspace .flex.items-end.gap-0\\.5'), engineLoaderStatus: document.querySelector('#previewWorkspace section div div span.flex'), engineLoaderBar: document.querySelector('#previewWorkspace section div .h-1 div'), themeToggle: document.getElementById('themeToggle'), sunIcon: document.getElementById('sunIcon'), moonIcon: document.getElementById('moonIcon') }; } async init() { this.initElements(); this.loadSettings(); this.applyTheme(); languageManager.setSecondaryLanguage(this.state.translation); this.enableAllControls(); await this.loadSurahsList(); this.setupListeners(); await this.triggerFetch(); this.updateLoaderStatus("System Fully Loaded", "100%", "bg-emerald-500"); } loadSettings() { try { const saved = localStorage.getItem('al_bayan_settings'); if (saved) { const parsed = JSON.parse(saved); this.state = { ...this.state, ...parsed }; } } catch (e) { console.error("UIController: Failed to load local settings", e); } } saveSettings() { try { const { activeVerseData, isPlayingAudio, ...toSave } = this.state; localStorage.setItem('al_bayan_settings', JSON.stringify(toSave)); } catch (e) { console.error("UIController: Failed to save local settings", e); } } applyTheme() { if (this.state.theme === 'light') { document.body.classList.add('light-theme'); if (this.els.sunIcon) this.els.sunIcon.classList.remove('hidden'); if (this.els.moonIcon) this.els.moonIcon.classList.add('hidden'); } else { document.body.classList.remove('light-theme'); if (this.els.sunIcon) this.els.sunIcon.classList.add('hidden'); if (this.els.moonIcon) this.els.moonIcon.classList.remove('hidden'); } } enableAllControls() { Object.keys(this.els).forEach(key => { const el = this.els[key]; if (el) { if (el instanceof NodeList || el instanceof HTMLCollection) { el.forEach(item => { if (item instanceof HTMLElement) { item.removeAttribute('disabled'); item.classList.remove('cursor-not-allowed'); } }); } else if (el instanceof HTMLElement) { el.removeAttribute('disabled'); el.classList.remove('cursor-not-allowed'); } } }); } async loadSurahsList() { if (!this.els.surahSelect) return; this.updateLoaderStatus("Loading Surah Catalog...", "30%", "bg-gold-500"); const chapters = await quranService.getSurahs(); this.chapters = chapters; this.els.surahSelect.innerHTML = ''; chapters.forEach(ch => { const opt = document.createElement('option'); opt.value = ch.id; opt.textContent = `${ch.id}. ${ch.name} (${ch.arabic})`; if (ch.id === this.state.surah) opt.selected = true; this.els.surahSelect.appendChild(opt); }); this.updateVerseLimits(); } updateVerseLimits() { if (!this.chapters || !this.els.startVerseInput || !this.els.endVerseInput) return; const selectedSurahId = this.state.surah; const ch = this.chapters.find(c => c.id === selectedSurahId); if (ch) { const maxVerses = ch.versesCount || 286; this.els.startVerseInput.max = maxVerses; this.els.endVerseInput.max = maxVerses; let startVal = parseInt(this.els.startVerseInput.value) || 1; let endVal = parseInt(this.els.endVerseInput.value) || 1; startVal = Math.max(1, Math.min(maxVerses, startVal)); endVal = Math.max(1, Math.min(maxVerses, endVal)); if (startVal > endVal) startVal = endVal; this.state.startVerse = startVal; this.state.endVerse = endVal; this.els.startVerseInput.value = startVal; this.els.endVerseInput.value = endVal; } } updateLoaderStatus(text, width, barColor = "bg-gold-500") { if (this.els.engineLoaderStatus) { this.els.engineLoaderStatus.innerHTML = ` <span class="h-2 w-2 rounded-full ${barColor === 'bg-emerald-500' ? 'bg-emerald-500' : 'bg-gold-400 animate-ping'}"></span> <span>${text}</span> `; } if (this.els.engineLoaderBar) { this.els.engineLoaderBar.style.width = width; this.els.engineLoaderBar.className = `h-full ${barColor} rounded-full transition-all duration-300`; } } setupListeners() { if (this.els.themeToggle) { this.els.themeToggle.addEventListener('click', () => { this.state.theme = this.state.theme === 'dark' ? 'light' : 'dark'; this.applyTheme(); this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.surahSelect) { this.els.surahSelect.addEventListener('change', (e) => { this.state.surah = parseInt(e.target.value); this.updateVerseLimits(); this.saveSettings(); }); } if (this.els.startVerseInput) { this.els.startVerseInput.addEventListener('input', (e) => { const max = parseInt(this.els.startVerseInput.max) || 286; let val = parseInt(e.target.value) || 1; val = Math.max(1, Math.min(max, val)); if (val > this.state.endVerse) { this.state.endVerse = val; if (this.els.endVerseInput) this.els.endVerseInput.value = val; } this.state.startVerse = val; e.target.value = val; this.saveSettings(); }); } if (this.els.endVerseInput) { this.els.endVerseInput.addEventListener('input', (e) => { const max = parseInt(this.els.endVerseInput.max) || 286; let val = parseInt(e.target.value) || 1; val = Math.max(1, Math.min(max, val)); if (val < this.state.startVerse) { this.state.startVerse = val; if (this.els.startVerseInput) this.els.startVerseInput.value = val; } this.state.endVerse = val; e.target.value = val; this.saveSettings(); }); } if (this.els.translationSelect) { this.els.translationSelect.value = this.state.translation; this.els.translationSelect.addEventListener('change', (e) => { this.state.translation = e.target.value; languageManager.setSecondaryLanguage(e.target.value); this.saveSettings(); this.triggerFetch(); }); } if (this.els.fetchButton) { this.els.fetchButton.addEventListener('click', () => this.triggerFetch()); } if (this.els.contrastSlider) { this.els.contrastSlider.value = this.state.contrast; this.els.contrastSlider.addEventListener('input', (e) => { this.state.contrast = parseInt(e.target.value); const valSpan = this.els.contrastSlider.parentElement.querySelector('span.text-gold-400'); if (valSpan) valSpan.textContent = `${this.state.contrast}%`; this.updateMockupPreview(); this.saveSettings(); }); } if (this.els.arabicFontSelect) { this.els.arabicFontSelect.value = this.state.arabicFont; this.els.arabicFontSelect.addEventListener('change', (e) => { this.state.arabicFont = e.target.value; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.translationFontSelect) { this.els.translationFontSelect.value = this.state.translationFont; this.els.translationFontSelect.addEventListener('change', (e) => { this.state.translationFont = e.target.value; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.arabicSizeSlider) { this.els.arabicSizeSlider.value = this.state.arabicSize; this.els.arabicSizeSlider.addEventListener('input', (e) => { this.state.arabicSize = parseInt(e.target.value); const valSpan = this.els.arabicSizeSlider.parentElement.querySelector('span.text-gold-400'); if (valSpan) valSpan.textContent = `${this.state.arabicSize}px`; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.translationSizeSlider) { this.els.translationSizeSlider.value = this.state.translationSize; this.els.translationSizeSlider.addEventListener('input', (e) => { this.state.translationSize = parseInt(e.target.value); const valSpan = this.els.translationSizeSlider.parentElement.querySelector('span.text-gold-400'); if (valSpan) valSpan.textContent = `${this.state.translationSize}px`; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.diacriticsCheckbox) { this.els.diacriticsCheckbox.checked = this.state.diacritics; this.els.diacriticsCheckbox.addEventListener('change', (e) => { this.state.diacritics = e.target.checked; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.goldenBorderCheckbox) { this.els.goldenBorderCheckbox.checked = this.state.goldenBorder; this.els.goldenBorderCheckbox.addEventListener('change', (e) => { this.state.goldenBorder = e.target.checked; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.showReferenceCheckbox) { this.els.showReferenceCheckbox.checked = this.state.showReference; this.els.showReferenceCheckbox.addEventListener('change', (e) => { this.state.showReference = e.target.checked; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.fontGapSlider) { this.els.fontGapSlider.value = this.state.fontGap || 0; const fontGapValSpan = document.getElementById('fontGapVal'); this.els.fontGapSlider.addEventListener('input', (e) => { this.state.fontGap = parseInt(e.target.value) || 0; if (fontGapValSpan) fontGapValSpan.textContent = `${this.state.fontGap}px`; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.lineGapSlider) { this.els.lineGapSlider.value = this.state.lineGap || 0; const lineGapValSpan = document.getElementById('lineGapVal'); this.els.lineGapSlider.addEventListener('input', (e) => { this.state.lineGap = parseInt(e.target.value) || 0; if (lineGapValSpan) lineGapValSpan.textContent = `${this.state.lineGap}px`; this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.shuffleBgBtn) { this.els.shuffleBgBtn.addEventListener('click', () => { this.state.bgSeed = Math.floor(Math.random() * 1000000); this.saveSettings(); this.updateMockupPreview(); }); } if (this.els.reciterSelect) { this.els.reciterSelect.value = this.state.reciter; this.els.reciterSelect.addEventListener('change', (e) => { this.state.reciter = e.target.value; this.saveSettings(); this.triggerFetch(); }); } if (this.els.ratioButtons) { this.els.ratioButtons.forEach(btn => { btn.addEventListener('click', () => { const text = btn.textContent.trim(); if (text.includes('1:1')) this.state.aspectRatio = '1:1'; if (text.includes('9:16')) this.state.aspectRatio = '9:16'; if (text.includes('16:9')) this.state.aspectRatio = '16:9'; this.saveSettings(); this.adjustPreviewContainerRatio(); }); }); } if (this.els.audioPlayBtn) { this.els.audioPlayBtn.addEventListener('click', () => this.toggleAudioPlay()); } this.audioElement.addEventListener('timeupdate', () => this.updateAudioProgress()); this.audioElement.addEventListener('ended', () => { this.playNextVerse(); }); } adjustPreviewContainerRatio() { if (!this.els.mockupContainer) return; this.els.mockupContainer.className = 'w-full max-w-[380px] rounded-xl overflow-hidden shadow-2xl shadow-black relative border border-gold-500/25 transition-all duration-300'; if (this.state.aspectRatio === '1:1') this.els.mockupContainer.classList.add('aspect-square'); else if (this.state.aspectRatio === '9:16') this.els.mockupContainer.classList.add('aspect-[9/16]'); else if (this.state.aspectRatio === '16:9') this.els.mockupContainer.classList.add('aspect-[16/9]', 'max-w-[500px]'); } async triggerFetch() { this.updateLoaderStatus("Fetching Quranic Data...", "60%", "bg-gold-500"); try { const surahNameFallback = this.chapters.find(c => c.id === this.state.surah)?.name || ""; const verses = await quranService.fetchVerses(this.state.surah, this.state.startVerse, this.state.endVerse, this.state.translation, this.state.reciter, surahNameFallback); if (verses && verses.length > 0) { this.state.activeVerseData = combineVerses(verses, this.state.translation); this.state.currentVerseIdx = 0; this.updateLoaderStatus("Data Synced Successfully", "100%", "bg-emerald-500"); this.updateMockupPreview(); this.loadAudioSource(); } else { throw new Error("No verses returned"); } } catch (err) { console.error("UIController: Failed to fetch verse data", err); this.updateLoaderStatus("Fetch Failed - Retrying...", "100%", "bg-rose-500"); setTimeout(() => this.triggerFetch(), 2000); } } loadAudioSource() { if (!this.state.activeVerseData?.verses?.[this.state.currentVerseIdx]?.audio) return; this.audioElement.src = this.state.activeVerseData.verses[this.state.currentVerseIdx].audio; this.audioElement.load(); } playNextVerse() { if (!this.state.activeVerseData?.verses) return; this.state.currentVerseIdx++; if (this.state.currentVerseIdx < this.state.activeVerseData.verses.length) { this.loadAudioSource(); this.audioElement.play().catch(e => console.warn("Audio play blocked", e)); } else { this.state.isPlayingAudio = false; this.state.currentVerseIdx = 0; this.loadAudioSource(); this.updateAudioBtnUI(); } } toggleAudioPlay() { if (!this.state.activeVerseData) return; if (this.state.isPlayingAudio) { this.audioElement.pause(); this.state.isPlayingAudio = false; } else { if (this.state.currentVerseIdx >= this.state.activeVerseData.verses.length) { this.state.currentVerseIdx = 0; this.loadAudioSource(); } this.audioElement.play().catch(e => console.warn("Audio play blocked", e)); this.state.isPlayingAudio = true; } this.updateAudioBtnUI(); } updateAudioBtnUI() { if (!this.els.audioPlayBtn) return; if (this.state.isPlayingAudio) { this.els.audioPlayBtn.innerHTML = `<svg class="h-3 w-3 fill-current" viewBox="0 0 24 24"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/></svg>`; this.els.audioPlayBtn.classList.add('bg-gold-500', 'text-slate-950'); } else { this.els.audioPlayBtn.innerHTML = `<svg class="h-3 w-3 fill-current" viewBox="0 0 24 24"><path d="M8 5v14l11-7z" /></svg>`; this.els.audioPlayBtn.classList.remove('bg-gold-500', 'text-slate-950'); } } updateAudioProgress() { if (!this.els.audioTimer || !this.audioElement) return; const cur = this.audioElement.currentTime || 0; const dur = this.audioElement.duration || 15; const formatTime = (s) => `${Math.floor(s / 60)}:${Math.floor(s % 60).toString().padStart(2, '0')}`; this.els.audioTimer.textContent = `${formatTime(cur)} / ${formatTime(dur)}`; const progressBar = this.els.mockupContainer?.querySelector('.absolute.bottom-0 div'); if (progressBar) progressBar.style.width = `${(cur / dur) * 100}%`; } updateMockupPreview() { if (!this.state.activeVerseData || !this.els.mockupContainer) return; const layout = languageManager.getLayoutPlan(this.state.activeVerseData, { arabicSize: this.state.arabicSize, translationSize: this.state.translationSize }); const arabicTextEl = this.els.mockupContainer.querySelector('.font-serif.text-2xl'); const englishTextEl = this.els.mockupContainer.querySelector('.font-sans.text-xs'); const referenceEl = this.els.mockupContainer.querySelector('.font-display.text-\\[9px\\]'); if (arabicTextEl) { let displayAr = layout.arabic.text; if (!this.state.diacritics) displayAr = displayAr.replace(/[\u064B-\u0652]/g, ""); const fontMap = { 'Amiri': '"Amiri", serif', 'Reem Kufi': '"Reem Kufi", sans-serif', 'Scheherazade': '"Scheherazade New", serif', 'Noto Naskh Arabic': '"Noto Naskh Arabic", serif', 'Lateef': '"Lateef", serif', 'Harmattan': '"Harmattan", sans-serif' }; arabicTextEl.style.fontFamily = fontMap[this.state.arabicFont] || fontMap['Amiri']; arabicTextEl.innerHTML = `<div style="font-size: ${layout.arabic.fontSize}px; word-spacing: ${this.state.fontGap}px; line-height: ${layout.arabic.fontSize * 1.5 + this.state.lineGap}px;">${displayAr}</div>`; } if (englishTextEl) { const fontMap = { 'Plus Jakarta Sans': '"Plus Jakarta Sans", sans-serif', 'Cinzel': '"Cinzel", serif', 'Montserrat': '"Montserrat", sans-serif', 'Playfair Display': '"Playfair Display", serif', 'Lora': '"Lora", serif', 'Georgia': 'Georgia, serif' }; englishTextEl.style.fontFamily = fontMap[this.state.translationFont] || fontMap['Plus Jakarta Sans']; englishTextEl.innerHTML = `<div style="font-size: ${layout.translations.en.fontSize}px; word-spacing: ${this.state.fontGap}px; line-height: ${layout.translations.en.fontSize * 1.45 + this.state.lineGap}px;">${layout.translations.en}</div>`; } if (referenceEl) referenceEl.textContent = layout.meta.reference; const backgroundDiv = this.els.mockupContainer.querySelector('.absolute.inset-0.bg-cover'); if (backgroundDiv) { const bgUrl = unsplashService.getBgImageSync(this.state.bgSeed || 12345, false); const overlayColor = this.state.theme === 'light' ? 'rgba(200, 200, 200, 0.4)' : 'rgba(16, 33, 30, 0.4)'; const bottomColor = this.state.theme === 'light' ? 'rgba(248, 250, 252, 0.95)' : 'rgba(10, 10, 10, 0.95)'; backgroundDiv.style.backgroundImage = `linear-gradient(180deg, ${overlayColor} 0%, ${bottomColor} 100%), url('${bgUrl}')`; } const darkOverlay = this.els.mockupContainer.querySelector('.absolute.inset-0.bg-slate-950\\/40'); if (darkOverlay) { // Always use dark overlay for contrast regardless of theme darkOverlay.style.backgroundColor = `rgba(2, 6, 23, ${this.state.contrast / 100})`; } } } export const uiController = new UIController();